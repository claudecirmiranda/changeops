{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "ensure-docker-running",
			"type": "shell",
			"command": "docker info",
			"windows": {
				"command": "docker info >$null 2>&1; if ($LASTEXITCODE -ne 0) { Write-Host 'Docker not running, starting Docker Desktop...'; Start-Process 'C:\\Program Files\\Docker\\Docker\\Docker Desktop.exe'; Write-Host 'Waiting for Docker to be ready...'; $timeout=60; $elapsed=0; while ($elapsed -lt $timeout) { Start-Sleep -Seconds 2; docker info >$null 2>&1; if ($LASTEXITCODE -eq 0) { Write-Host 'Docker is ready!'; break }; $elapsed+=2; Write-Host ('Waiting... ' + $elapsed + 's') }; if ($elapsed -ge $timeout) { Write-Error 'Docker failed to start within 60s'; exit 1 } } else { Write-Host 'Docker is already running.' }"
			},
			"linux": {
				"command": "docker info > /dev/null 2>&1 || (echo 'Docker is not running. Start the Docker daemon before running integration tests.' && exit 1)"
			},
			"osx": {
				"command": "docker info > /dev/null 2>&1 || (echo 'Docker is not running. Start Docker Desktop before running integration tests.' && exit 1)"
			},
			"isBackground": false,
			"problemMatcher": [],
			"presentation": {
				"reveal": "silent",
				"showReuseMessage": false
			}
		},
		{
			"label": "test-all-backend",
			"type": "shell",
			"command": "cd backend/change-service && mvn test && cd ../deploy-orchestrator && mvn test",
			"isBackground": false,
			"problemMatcher": [],
			"group": "test"
		},
		{
			"label": "checkstyle-deploy-orchestrator",
			"type": "shell",
			"command": "cd backend/deploy-orchestrator && mvn -B checkstyle:check",
			"isBackground": false,
			"group": "test"
		}
	]
}