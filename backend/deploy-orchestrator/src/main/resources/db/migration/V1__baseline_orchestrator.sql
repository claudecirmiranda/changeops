-- V1__baseline_orchestrator.sql
-- deploy-orchestrator uses the shared 'changeops' database
-- processed_events is already created by change-service V1 migration
-- This migration is a no-op baseline so Flyway has a starting point

SELECT 1;
